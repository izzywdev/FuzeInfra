# FuzeInfra Platform - Git Ignore File

# =============================================================================
# ENVIRONMENT AND SECRETS
# =============================================================================
.env
.env.*
!.env.template
!environment.template
*.pem
# EXCEPTION: the Sealed Secrets controller's PUBLIC cert. It encrypts only — the
# private key never leaves the cluster — and seal-secret.sh falls back to it when
# the published cert URL is unreachable (it sits behind Cloudflare Access), so it
# MUST be committed or offline sealing breaks. Refresh after a key rotation with
#   kubeseal --controller-namespace kube-system #            --controller-name sealed-secrets-controller --fetch-cert #     > deploy/sealed-secrets/sealing-cert.pem
*.key
secrets/
config/secrets/

# Terraform secrets and provider binaries
terraform.tfvars
*.tfvars.json
.terraform/providers/
.terraform/plugins/

# Extracted kubeconfig from terraform/contabo provisioning
terraform/contabo/k3s-kubeconfig.yaml

# Terraform state and provider cache (contabo)
terraform/contabo/terraform.tfstate
terraform/contabo/terraform.tfstate.backup
terraform/contabo/.terraform/

# Downloaded binaries (tools fetched at runtime)
mkcert-v*

# SSH Keys Directory - track structure but not key files
keys/*.pem
keys/*.key
keys/*_key
keys/*.old
!keys/.gitkeep
!keys/README.md

# =============================================================================
# LOGS AND RUNTIME
# =============================================================================
logs/
*.log
log/
*.log.*
*.out
pid
*.pid
*.seed
*.pid.lock

# =============================================================================
# DATABASE FILES
# =============================================================================
*.db
*.sqlite
*.sqlite3
data/
volumes/
postgres_data/
mongodb_data/
redis_data/
neo4j_data/
elasticsearch_data/

# =============================================================================
# DEPENDENCY DIRECTORIES
# =============================================================================
node_modules/
__pycache__/
*.py[cod]
*$py.class
*.pyc
venv/
.venv/
env/
.env/
.virtualenv/

# =============================================================================
# BUILD AND DISTRIBUTION
# =============================================================================
build/
dist/
*.egg-info/
.eggs/
target/
out/

# =============================================================================
# IDE AND EDITOR FILES
# =============================================================================
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
*.tmp
*.temp

# =============================================================================
# DOCKER AND CONTAINERS
# =============================================================================
docker-compose.override.yml
.docker/
volumes/

# =============================================================================
# MONITORING AND METRICS
# =============================================================================
prometheus_data/
grafana_data/
loki_data/
alertmanager_data/

# =============================================================================
# BACKUP FILES
# =============================================================================
*.backup
*.bak
backups/
dump/
*.dump
*.sql.gz

# =============================================================================
# OS GENERATED FILES
# =============================================================================
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# =============================================================================
# CERTIFICATES AND KEYS
# =============================================================================
*.crt
*.key
*.pem
*.p12
*.pfx
certs/
ssl/

# =============================================================================
# TEMPORARY AND CACHE FILES
# =============================================================================
.cache/
.pytest_cache/
.coverage
htmlcov/
.tox/
.nox/
coverage.xml
*.cover
.hypothesis/

# =============================================================================
# DEVELOPMENT AND TESTING
# =============================================================================
.pytest_cache/
.coverage
htmlcov/
test-results/
coverage/

# =============================================================================
# DOCUMENTATION BUILD
# =============================================================================
docs/_build/
site/

# =============================================================================
# LOCAL DEVELOPMENT OVERRIDES
# =============================================================================
docker-compose.local.yml
local-config/
dev-overrides/ 

# ── Sealed Secrets PUBLIC cert (must be last: last matching pattern wins) ─────
# There are multiple `*.pem` rules above; this negation has to come after all of
# them or git keeps ignoring the file. This cert is PUBLIC — it encrypts only,
# the private key never leaves the cluster. seal-secret.sh falls back to it when
# the published cert URL is unreachable (Cloudflare Access), so it MUST be
# committed or offline sealing breaks. Refresh after a controller key rotation:
#   kubeseal --controller-namespace kube-system #            --controller-name sealed-secrets-controller --fetch-cert #     > deploy/sealed-secrets/sealing-cert.pem
!deploy/sealed-secrets/sealing-cert.pem
