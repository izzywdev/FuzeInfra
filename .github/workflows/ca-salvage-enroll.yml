name: ca-salvage-enroll

# Salvage already-paid Contabo instances into the elastic pool: reinstall each
# with the elastic cloud-init (userData sent PLAIN — Contabo does NOT want it
# base64-encoded; that double-encoding was the "provisioned-but-never-joined"
# bug) and assign the active pool tag so the autoscaler adopts them.
# Also the LIVE validation of the plain-userData fix: dry-run, then reinstall
# ONE running orphan and confirm it joins `kubectl get nodes`.
#
# Safety: this workflow previously accepted durable/control-plane templates and
# arbitrary node-name overrides while still assigning the autoscaler's tag.
# That contaminated desired-size discovery with baseline/CI instances and
# blocked every CA loop. The active tag/prefix are pinned below and guarded by
# tests/test_autoscaler_identity.py; only elastic templates and matching names
# may now be enrolled.
on:
  workflow_dispatch:
    inputs:
      instance_ids:
        description: 'Comma-separated Contabo instance IDs to reinstall + enroll as elastic nodes.'
        default: ''
        required: true
      mode:
        description: 'dry-run = render + validate (safe); reinstall = PUT reinstall (plain userData) + tag'
        type: choice
        options: [dry-run, reinstall]
        default: dry-run
        required: true
      confirm:
        description: 'For mode=reinstall ONLY: type SALVAGE to proceed.'
        default: ''
      template:
        description: 'Elastic cloud-init template path (non-elastic templates are rejected).'
        default: 'cluster-autoscaler/contabo-externalgrpc/deploy/elastic-userdata.template'
      node_name:
        description: 'Override k8s --node-name. Must start with the active elastic prefix; single-instance only.'
        default: ''

permissions:
  contents: read

jobs:
  salvage:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v4
      - name: Reinstall + enroll elastic instances
        env:
          CID: ${{ secrets.CONTABO_CLIENT_ID }}
          CSEC: ${{ secrets.CONTABO_CLIENT_SECRET }}
          CUSER: ${{ secrets.CONTABO_API_USER }}
          CPASS: ${{ secrets.CONTABO_API_PASSWORD }}
          IMAGE_ID: ${{ secrets.CONTABO_IMAGE_ID }}
          K3S_SERVER_URL: ${{ secrets.K3S_SERVER_URL }}
          K3S_NODE_TOKEN: ${{ secrets.K3S_NODE_TOKEN }}
          NODE_SSH_PUBLIC_KEY: ${{ secrets.NODE_SSH_PUBLIC_KEY }}
          # Needed only to clear the stale node-password secret before a reinstall
          # (see the comment at that step). The workflow degrades to a warning if
          # it is absent rather than failing the reinstall.
          KUBE_CONFIG: ${{ secrets.KUBE_CONFIG }}
          IDS: ${{ inputs.instance_ids }}
          MODE: ${{ inputs.mode }}
          CONFIRM: ${{ inputs.confirm }}
          TEMPLATE_IN: ${{ inputs.template }}
          NODE_NAME_OVERRIDE: ${{ inputs.node_name }}
          ELASTIC_TAG: fuzeinfra-prod-elastic-v2
          ELASTIC_NAME_PREFIX: fuzeinfra-prod-elastic-v2
        run: |
          set -euo pipefail
          TMPL="${TEMPLATE_IN:-cluster-autoscaler/contabo-externalgrpc/deploy/elastic-userdata.template}"
          [ -f "$TMPL" ] || { echo "::error::template not found: $TMPL"; exit 1; }
          case "$(basename "$TMPL")" in
            elastic-userdata*.template) ;;
            *)
              echo "::error::refusing to enroll a non-elastic template '$TMPL' into autoscaler pool '$ELASTIC_TAG'"
              exit 1
              ;;
          esac
          for v in CID CSEC CUSER CPASS IMAGE_ID K3S_SERVER_URL K3S_NODE_TOKEN NODE_SSH_PUBLIC_KEY; do
            eval "val=\${$v}"; [ -n "$val" ] || { echo "::error::secret/env $v is empty"; exit 1; }
          done
          if [ "$MODE" = "reinstall" ] && [ "$CONFIRM" != "SALVAGE" ]; then
            echo "::error::mode=reinstall requires confirm=SALVAGE"; exit 1
          fi

          TOK=$(curl -sS -X POST -H "Content-Type: application/x-www-form-urlencoded" \
            "https://auth.contabo.com/auth/realms/contabo/protocol/openid-connect/token" \
            --data-urlencode "client_id=${CID}" --data-urlencode "client_secret=${CSEC}" \
            --data-urlencode "username=${CUSER}" --data-urlencode "password=${CPASS}" \
            --data-urlencode "grant_type=password" | jq -r .access_token)
          [ -n "$TOK" ] && [ "$TOK" != "null" ] || { echo "::error::Contabo auth failed"; exit 1; }

          TAGID=$(curl -sS -H "Authorization: Bearer $TOK" -H "x-request-id: $(cat /proc/sys/kernel/random/uuid)" \
            "https://api.contabo.com/v1/tags?name=${ELASTIC_TAG}" \
            | jq -r --arg name "$ELASTIC_TAG" '.data[] | select(.name == $name) | .tagId' | head -1)
          if [ -z "$TAGID" ]; then
            TAG_BODY=$(jq -nc --arg name "$ELASTIC_TAG" '{name:$name,color:"#00aaaa"}')
            TAGID=$(curl -sS -X POST -H "Authorization: Bearer $TOK" -H "Content-Type: application/json" \
              -H "x-request-id: $(cat /proc/sys/kernel/random/uuid)" \
              -d "$TAG_BODY" \
              "https://api.contabo.com/v1/tags" | jq -r '.data[0].tagId')
          fi
          [ -n "$TAGID" ] && [ "$TAGID" != "null" ] || { echo "::error::could not resolve/create exact tag '$ELASTIC_TAG'"; exit 1; }
          echo "$ELASTIC_TAG tagId=$TAGID"

          IFS=',' read -ra ARR <<< "$IDS"
          for RAW in "${ARR[@]}"; do
            ID=$(echo "$RAW" | tr -dc '0-9')
            [ -n "$ID" ] || continue
            INST=$(curl -sS -H "Authorization: Bearer $TOK" -H "x-request-id: $(cat /proc/sys/kernel/random/uuid)" \
              "https://api.contabo.com/v1/compute/instances/${ID}")
            NAME=$(echo "$INST" | jq -r '.data[0].displayName')
            [ -n "$NODE_NAME_OVERRIDE" ] && NAME="$NODE_NAME_OVERRIDE"
            STATUS=$(echo "$INST" | jq -r '.data[0].status')
            [ -n "$NAME" ] && [ "$NAME" != "null" ] || { echo "::warning::instance $ID not found, skipping"; continue; }
            case "$NAME" in
              "$ELASTIC_NAME_PREFIX"-*) ;;
              *)
                echo "::error::refusing to tag instance $ID: node name '$NAME' is outside active elastic prefix '$ELASTIC_NAME_PREFIX-'"
                exit 1
                ;;
            esac
            echo "==== instance $ID  name=$NAME  status=$STATUS ===="

            # render cloud-init (sed; token/key never printed). '|' delim: replacements contain / + = : @ but no |.
            RENDERED=$(sed \
              -e "s|{{.NodeName}}|${NAME}|g" \
              -e "s|{{.K3SServerURL}}|${K3S_SERVER_URL}|g" \
              -e "s|{{.K3SNodeToken}}|${K3S_NODE_TOKEN}|g" \
              -e "s|__SSH_PUBLIC_KEY__|${NODE_SSH_PUBLIC_KEY}|g" \
              "$TMPL")
            if printf '%s' "$RENDERED" | grep -q '{{'; then echo "::error::unrendered template action remains for $ID"; exit 1; fi
            if ! printf '%s' "$RENDERED" | head -1 | grep -q '^#cloud-config'; then echo "::error::rendered userData must start with #cloud-config"; exit 1; fi
            echo "  render checks:"
            for m in "INSTALL_K3S_CHANNEL=v1.36" "ufw allow 51820/udp" "get.k3s.io" "--node-name '${NAME}'" "provider-id=contabo://${NAME}"; do
              # `--` before the pattern: two of these markers START with "--", so
              # without it grep parses the pattern as an option bundle, exits 2, and
              # the check prints a FALSE "MISS" for a template that rendered
              # perfectly. Reproduced 2026-09-07 while chasing a VLAN reinstall that
              # was not broken -- the render check was.
              if printf '%s' "$RENDERED" | grep -qF -- "$m"; then echo "    OK   $m"; else echo "    MISS $m"; fi
            done

            if [ "$MODE" = "dry-run" ]; then
              echo "  [dry-run] WOULD PUT reinstall imageId=$IMAGE_ID (userData PLAIN, $(printf '%s' "$RENDERED" | wc -c) bytes) + assign tag $ELASTIC_TAG/$TAGID to $ID"
              continue
            fi

            # Drop the k3s node-password secret BEFORE wiping the host.
            #
            # k3s stores a per-node secret `<node>.node-password.k3s` in kube-system
            # and checks it on every agent registration. A reinstall gives the host a
            # brand-new random password while that secret survives in etcd, so the
            # rejoin is refused with "Node password rejected, duplicate hostname" and
            # the node can NEVER come back under its own name. Nothing in the reinstall
            # path cleaned this up, which is the likely explanation for the recurring
            # "reinstalled fine but never joined" reports -- the instance was healthy
            # and the cluster was rejecting it.
            #
            # Best-effort: a missing secret is normal for a first enrolment, and if
            # kubectl cannot reach the cluster we still want the reinstall to proceed
            # (the operator can delete the secret by hand) rather than block on it.
            if [ -n "${KUBE_CONFIG:-}" ]; then
              mkdir -p ~/.kube && printf '%s' "$KUBE_CONFIG" | base64 -d > ~/.kube/config 2>/dev/null && chmod 600 ~/.kube/config
              if kubectl --request-timeout=20s delete secret -n kube-system "${NAME}.node-password.k3s" --ignore-not-found 2>&1; then
                echo "  node-password secret cleared for $NAME (rejoin will be accepted)"
              else
                echo "::warning::could not clear ${NAME}.node-password.k3s -- if the node does not rejoin, delete it by hand"
              fi
            else
              echo "::warning::KUBE_CONFIG unset -- cannot clear ${NAME}.node-password.k3s; a rejoin under the same name may be refused"
            fi

            BODY=$(jq -n --arg img "$IMAGE_ID" --arg ud "$RENDERED" '{imageId:$img, userData:$ud, defaultUser:"root"}')
            RC=$(curl -sS -o /tmp/reinstall.json -w '%{http_code}' -X PUT \
              -H "Authorization: Bearer $TOK" -H "Content-Type: application/json" \
              -H "x-request-id: $(cat /proc/sys/kernel/random/uuid)" \
              -d "$BODY" "https://api.contabo.com/v1/compute/instances/${ID}")
            echo "  reinstall $ID -> HTTP $RC"
            if [ "$RC" != "200" ] && [ "$RC" != "201" ]; then echo "  body: $(cat /tmp/reinstall.json)"; echo "::warning::reinstall $ID failed"; continue; fi
            TC=$(curl -sS -o /dev/null -w '%{http_code}' -X POST \
              -H "Authorization: Bearer $TOK" -H "x-request-id: $(cat /proc/sys/kernel/random/uuid)" \
              "https://api.contabo.com/v1/tags/${TAGID}/assignments/instance/${ID}")
            echo "  tag $ID -> HTTP $TC"
            echo "  instance $ID ($NAME) reinstalling with PLAIN userData; should run cloud-init + join as node '$NAME' in a few minutes."
          done
          echo "===== done (mode=$MODE) ====="
