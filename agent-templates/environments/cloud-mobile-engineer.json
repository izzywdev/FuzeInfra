{
  "$schema": "../schema/environment.schema.json",
  "name": "fuzeinfra-cloud-mobile-engineer",
  "config": {
    "type": "cloud",
    "packages": {
      "apt": ["jq"],
      "npm": ["playwright", "@lhci/cli", "workbox-cli", "@capacitor/cli"],
      "pip": ["pyyaml"]
    },
    "networking": {
      "type": "limited",
      "allowed_hosts": [],
      "allow_mcp_servers": true,
      "allow_package_managers": true
    }
  }
}
