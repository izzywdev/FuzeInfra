{
  "$schema": "../schema/environment.schema.json",
  "name": "fuzeinfra-cloud-fuze",
  "config": {
    "type": "cloud",
    "packages": {
      "apt": ["gh"],
      "npm": ["prettier"],
      "go": ["github.com/yannh/kubeconform/cmd/kubeconform@latest"],
      "pip": [
        "pytest",
        "pytest-asyncio",
        "requests",
        "httpx",
        "pyyaml",
        "yamllint",
        "check-jsonschema",
        "mcp>=1.9,<2",
        "websockets"
      ]
    },
    "networking": {
      "type": "limited",
      "allowed_hosts": ["get.helm.sh", "github.com", "*.fuzefront.com"],
      "allow_mcp_servers": true,
      "allow_package_managers": true
    }
  }
}
