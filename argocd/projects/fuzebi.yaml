apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: fuzebi
  namespace: argocd
spec:
  description: FuzeBI product (consumer of FuzeInfra) — may deploy ONLY into its own namespace.
  sourceRepos:
    - https://github.com/izzywdev/FuzeBI.git
  destinations:
    - namespace: fuzebi
      server: https://kubernetes.default.svc
    - namespace: argocd
      server: https://kubernetes.default.svc
  clusterResourceWhitelist:
    - group: "*"
      kind: "*"
  namespaceResourceWhitelist:
    - group: "*"
      kind: "*"
