apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: fuzedeploy
  namespace: argocd
spec:
  description: FuzeDeploy product (consumer of FuzeInfra) — may deploy ONLY into its own namespace.
  sourceRepos:
    - https://github.com/izzywdev/FuzeDeploy.git
  destinations:
    - namespace: fuzedeploy
      server: https://kubernetes.default.svc
    - namespace: argocd
      server: https://kubernetes.default.svc
  clusterResourceWhitelist:
    - group: "*"
      kind: "*"
  namespaceResourceWhitelist:
    - group: "*"
      kind: "*"
